# Per-project: change `name` to your project's slug. Everything else can stay.
#
# NOTE: For Cloudflare Pages, this `name` field is essentially decorative —
# Pages identifies the project via the `--project-name` flag passed to
# `wrangler pages deploy`, OR via the project name configured in the
# Cloudflare dashboard. The two can differ.
# Always run `pnpm wrangler pages project list` to see the actual project
# name on Cloudflare. See DEPLOY.md → Path B → §1.B.1.
name = "wonderful-anysigma"
compatibility_date = "2026-05-11"
pages_build_output_dir = "dist"
